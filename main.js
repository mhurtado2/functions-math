const addStuff = (num1, num2 ) => {
    const sum = num1 + num2 
    return sum
}

let addition = addStuff(20, 14)
console.log(addition)

const multiplyStuff = (num1) => {
    const sum = num1 * 2 
    return sum
}

let multiplication = multiplyStuff(34)
console.log(multiplication)
